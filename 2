#include <stdio.h>

int main() {
    int sum = 0;
    int count = 0;

    for (int i = 0; i <= 30; i++) {
        sum += i;
        count++;
    }

    double average = (double)sum / count;
    printf("Середнє арифметичне: %lf\n", average);

    return 0;
}
