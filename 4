#include <stdio.h>

int main() {
    int sum = 0;
    int count = 0;
    int i = 0;

    do {
        sum += i;
        count++;
        i++;
    } while (i <= 30);

    double average = (double)sum / count;
    printf("Середнє арифметичне: %lf\n", average);

    return 0;
}
