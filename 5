#include <stdio.h>
#include <math.h>

int main() {
    int x;
    double d;

    printf("Введіть ціле число x: ");
    scanf("%d", &x);

    
    d = sqrt(x) + 2 * cbrt(x);

    printf("Результат обчислення: %lf\n", d);

    return 0;
}
